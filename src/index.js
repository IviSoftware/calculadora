import React from "react";
import ReactDOM  from "react-dom/client";
import App from './containers/App'

const rootPoint = document.getElementById('root');
const root = ReactDOM.createRoot(rootPoint);

root.render(
    <App />
);
